version: 2

models:
  - name: thing
    meta:
      foo: bar
  - name: use_variables
    description: demo to show variables
    columns:
      - name: order_item_key
        tests:
          - unique
          - not_null 
        