version: 2

models:
  - name: source_json
    columns:
      - name: id
      - name: type
        #tests:
        #  - accepted_values:
        #      values: ['web', 'mobile']
      - name: experiment_ids
      - name: experiment_mappings
        #tests:
        #  - not_null
              
