models:
  - name: my_table
    config:
      contract:
        enforced: true
    columns:
      - name: id
        data_type: int
      - name: bar
        data_type: varchar(3)
        constraints:
          - type: not_null